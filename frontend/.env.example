VITE_API_BASE_URL=http://localhost:8000

# Must match the backend's API_KEY once one is configured (required for any
# public deployment — see the root .env.example and README). Leave both
# empty for local development.
VITE_API_KEY=
